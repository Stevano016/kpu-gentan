# Project Memory — KPPS Gentan System

This file captures the active state, environment variables, completed tasks, and running processes to keep a clean history.

---

## 📍 Last Known State

- **Laravel Backend API (`/backend`)**: Running at `http://localhost:8000`. Added server-side validation to support four voter types (`dpt`, `dpk`, `dps`, `dptb`) in `DptController`. Adjusted `DashboardController` (`getSummary` and `getTpsDetails`) to compute stats for all four categories. Seeded 7,475 voters from Excel as `dp4` by default. Created a sync command `php artisan dpt:sync-csv` to update synthetic NIKs/NKKs to real ones in-place from updated Excel data.
- **Web Dashboard (`/web`)**: Running at `http://localhost:5173`. Added DPS and DPTb options in `DptModal.tsx` dropdown, filter buttons in `PemilihTab.tsx`, and extended components to display stats for four voter types. Updated Landing Page and Login Page footers with stacked watermark ("Ruang Komunitas Digital Desa \n Support by KKN Universitas Sugeng Hartono(USH) Kel-7 2026") where the KKN watermark is wrapped in a beautiful badge styled with an OKLCH primary-light background and thin border. Modified search error warning box with updated text, and integrated a "Syarat Pemilih" button that opens an rule-based criteria and Pantarlih assignment modal.
- **Mobile Client (`/mobile`)**: Analyzer is 100% clean (`No issues found!`). Extended `home_screen.dart` and `dashboard_tab.dart` to compute, validate, and display stats (Total and Check-in) for DPT, DPK, DPS, and DPTb.

---

## 📋 Task History & Context

- **15 Aug 2026**: Initialised Laravel 11 backend project, configured `php.ini` to enable SQLite support, created migrations, models, seeds, controllers, and tested migrations & seeders successfully.
- **15 Aug 2026**: Initialised React Vite (TS) project, configured modern OKLCH CSS theme, built out complete `App.tsx` containing authorization states, dashboard monitors (aggregate widgets, progress rings, quick count bars), TPS status tables, voter CRUD tables, bulk CSV voter upload handlers, and KPPS account provisioners.
- **15 Aug 2026**: Initialised Flutter mobile application, installed dependency extensions (`http`, `shared_preferences`, `path_provider`), implemented local offline cache storage, API syncing handlers, and screens (`LoginScreen`, `HomeScreen`).
- **15 Aug 2026**: Found host local IP (`192.168.11.9`), bound Laravel server to `0.0.0.0` (all interfaces) to allow external calls from physical devices, and updated Flutter API base URL configuration.
- **15 Aug 2026**: Added `mobile_scanner` to dependencies. Replaced TabBar with a clean `BottomNavigationBar` in Flutter. Created `ScannerScreen` for direct physical camera scanning and added input field fallback for PC/emulator testing. Passed `flutter analyze` with 0 issues.
- **15 Aug 2026**: Corrected case-sensitive namespace `chillerlan\QRCode\QRCode` in backend controller to resolve 500 error on QR Code endpoint.
- **15 Aug 2026**: Integrated QR download & printing buttons on web client. Redesigned Edit voter modal to include side-by-side QR preview. Configured voter creation success screen with immediate QR display.
- **15 Aug 2026**: Fixed Flutter sync bug by updating `isOnline` to check reachability of local PC IP (`192.168.11.9:8000`), allowing offline sync in local Wi-Fi sandboxes. Added auto-refresh of DPT cache if scanned NIK is not found locally.
- **15 Aug 2026**: Refactored `HomeScreen` state and views on Flutter to add a dynamic Dashboard tab for full access role. Implemented local calculation of Total DPT, Hadir DPT, and percentage. Added check-in metrics and attendance rate percentage in the web TPS table and optimized the backend query.
- **15 Aug 2026**: Renamed Android app to "Gentara", replaced all Android mipmap launcher icons with high-quality resampled `1786782031433.png`, and added a real-time Quick Count Results card with progress bars to the KPPS dashboard.
- **15 Aug 2026**: Fixed RenderFlex layout overflow in Quick Count input field headers and dashboard cards. Introduced stateful `_syncAction` to display button loaders during the "Simpan Draft" and "Submit Final" processes.
- **15 Aug 2026**: Added Daftar Pemilih Khusus (DPK) support. Extended backend controllers and dashboard APIs, created a DPK sidebar page and stats cards/donut breakdown widgets on the web client, and separated DPT/DPK metrics on the Android dashboard.
- **15 Aug 2026**: Switched check-in validation from NIK to unique voter ID (`USH-GTN-026xxxx`). Migrated backend SQLite database to add `id_pemilih` column and populated existing records. Updated manual and QR code scanner check-in in the Gentara app to search and validate by unique ID. Displayed the unique ID on both DPT and DPK web and mobile lists.
- **15 Aug 2026**: Updated database seeder to inject realistic dummy records containing DPK voters across all TPS. Wiped and re-seeded SQLite backend instance.
- **15 Aug 2026**: Paginated all lists (DPT, DPK, TPS, and KPPS Accounts) on the web dashboard and optimized backend database queries to prevent N+1 query patterns using eager-loading and subquery counters.
- **15 Aug 2026**: Implemented custom password eye toggle on web and mobile. Redesigned mobile alert popups to use a custom rounded Dialog UI. Standardized confirm popups on the web with a custom confirm modal overlay. Made the web panel fully responsive.
- **15 Aug 2026**: Fixed all `.section-header` headers on the web client to remain sticky/fixed at the top of the viewport when scrolling, with a background mask.
- **16 Aug 2026**: Registered `logo.png` image asset in Flutter mobile app, replaced placeholder icons on the login page and home page cards with larger, high-resolution logo assets, and added the logo to the AppBar. Replaced the web dashboard favicon and header icon references with `logo.png` for complete branding integration.
- **16 Aug 2026**: Fully refactored and modularized the web client, mobile client, and backend API as requested, without changing existing logic:
  - **Web Client**: Extracted the large monolithic `App.tsx` file (~1100 lines) into separate modular files: `Icons.tsx` (SVG assets), `CustomConfirmModal.tsx`, `api.ts` (REST client API services), `helpers.ts` (utility helpers), separate components for each view tab (`DashboardTab`, `TpsTab`, `TpsDetailTab`, `DptTab`, `DpkTab`, `KppsTab`), sidebar and loginscreen components, and all modal form components (`TpsModal`, `DptModal`, `ImportCsvModal`, `KppsModal`, `ResetPasswordModal`, `QrViewerModal`, `VoterSuccessModal`). Integrated `react-router-dom` for declarative routing (URLs like `/tps/:id`, login routes, and protected path navigations), removing custom page state routing, and compiled successfully without warnings/errors. Configured WebSocket client connection (ws://<host>:8080) to automatically refresh active dashboard views on update events. Fixed QR code download bug where files saved with a static .png extension instead of matching the underlying base64 SVG MIME type, preventing corrupted image file warnings in Windows Photos. Implemented candidate pairs (Paslon) management dashboard screen (/paslon), add/edit modal form, sidebar menu links, and integrated dynamic candidate name labels on both the general and detail dashboard panels.
  - **Laravel Backend**: Implemented route-level role-based access control using a custom `EnsureRole` middleware (registered in `bootstrap/app.php` and applied in `routes/api.php`). Cleaned up controllers (`DptController`, `TpsController`, `UserController`, `DashboardController`) by removing manual checks. Optimized CSV importer to auto-generate proper unique `id_pemilih` (with format `USH-GTN-026xxxx`) to ensure offline compatibility for Android. Checked PHP syntax with zero errors. Reinforced API security with custom rate limiters (throttle:login limits brute force on login; throttle:api limits general API requests) and parameter sanitization on synchronization endpoints in `SyncController.php`. Created a custom Artisan WebSocket command (`php artisan websocket:serve`) running on port 8080 with automated client handshake and TCP IPC message broadcasting, triggering updates on voter sync and quick count submits. Configured php-qrcode rendering options to output PNG format directly using QRGdImagePNG class wrapper. Added `paslons` database migration, Eloquent model, and `PaslonController` with full CRUD support, real-time WebSocket sync triggers, and seeded default candidate pair data.
  - **Mobile Client**: Extracted monolithic `home_screen.dart` (~1200 lines) by creating a `lib/screens/tabs/` folder containing dedicated widget files: `dashboard_tab.dart`, `validasi_tab.dart`, `quick_count_tab.dart`, and `status_sync_tab.dart`. Decoupled presentation and data states using callbacks. Verified using `flutter analyze` with zero warnings/errors. Added native WebSocket connection to ws://<host>:8080 with auto-reconnection and local database cache updates on sync events. Fixed QR scanning lookup bug to support both USH-GTN-026xxxx ID Pemilih and 16-digit NIK formats to maintain backward compatibility, and updated simulator view textfield guidelines.

- **16 Aug 2026**: Extended Data Pemilih schema with new demographic attributes (`umur`, `status_kawin`, `jenis_kelamin`, `alamat`, `rt`, `rw`, `pekerjaan`, `disabilitas`, `keterangan`). Implemented migrations, model update, controller CRUD validations, and realistic seeding logic on Laravel backend. Created a responsive form in `DptModal` and a collapsible drawer panel in `PemilihTab` to review details on the Web Client. Integrated demographic details display in the mobile check-in verification panel (`validasi_tab.dart`).
- **16 Aug 2026**: Implemented client-side and server-side validation to prevent quick count submissions from exceeding the total registered voters (DPT + DPK) and actual check-in (attendance) counts at that TPS. Verified that WebSocket real-time triggers are fully operational for quick count updates on the Web Dashboard.
- **16 Aug 2026**: Added new `nkk` attribute (Family Card Number) database migration, Eloquent model fillable array, DptController CRUD validation, web form field (DptModal.tsx), web details panel (PemilihTab.tsx), and mobile verification display (validasi_tab.dart). Built Python parser to extract, clean, drop duplicates, and export 6,856 voter records from `Data_Utama_Bersih (999) (1).xlsx` based on `NIK_Pembanding` and `NKK_Pembanding`, mapping `rt` and `rw` columns from `NO_RT_ALT` and `NO_RW_ALT`. Seeded 5 dynamic TPS based on partition rules in `image copy 2.png`, 10 KPPS user accounts, 2 Secretariat users, and 3 candidate pairs.
- **16 Aug 2026**: Added DPS (Daftar Pemilih Sementara) and DPTb (Daftar Pemilih Tambahan) categories across backend API validation and dashboard statistics controllers. Configured `DatabaseSeeder.php` to set all imported voters as `'dps'` by default. Added DPS and DPTb select and filter UI controls on React web dashboard (`DptModal.tsx`, `PemilihTab.tsx`, `DashboardTab.tsx`, `TpsDetailTab.tsx`) and verified compilation is clean. Refactored mobile client (`home_screen.dart` and `dashboard_tab.dart`) to count, display, and validate quick count bounds using all four voter types, passing `flutter analyze` successfully with zero warnings/errors. Fixed a bug in `PemilihTab.tsx` where voter type badge was hardcoded to display "DPT" instead of "DPS" or "DPTb". Rearranged filter buttons layout order to place DPS and DPTb next to "Semua Pemilih" first. Made React API and WebSocket endpoints resolve dynamically based on host location protocol and origin for seamless local development and production SSL deployments. Configured target PHP platform version 8.3.27 in `composer.json` and downgraded locked Composer packages to ensure compatibility with production server runtime.

---

- **17 Aug 2026 — Production deployment ke VPS `bleber` (`gentan.wujud.id`)**:
  - **Insiden server**: disk root 100% penuh (39G/39G) sejak 14 Aug 16:01 akibat `laravel.log` milik project lain (`/var/www/myapp/Sistem-Management`) membengkak jadi 16G. Efek berantai: `systemd-resolved` mati → `/etc/resolv.conf` hilang → DNS mati → `cloudflared` crash-loop → seluruh situs di VPS offline, dan `php8.2-fpm`/`php8.4-fpm` gagal start. Dipulihkan dengan: vacuum journald, truncate laravel.log (5000 baris terakhir disimpan ke `laravel.log.tail`), hapus worker.log lama + cache `/root` (.vscode-server, puppeteer, npm, composer), tulis ulang `/etc/resolv.conf` statis (1.1.1.1 / 8.8.8.8), restart resolved + cloudflared + php-fpm. Disk kembali ke 38% (25G bebas). Dipasang `/etc/logrotate.d/laravel-apps` (harian, 7 hari, maxsize 100M, copytruncate) untuk semua `storage/logs` Laravel di `/var/www`.
  - **Database**: migrasi dari SQLite ke MySQL karena PHP di server tidak punya ekstensi sqlite3. Dibuat `gentan_db` + user `gentan_user`@`localhost` (policy MySQL MEDIUM mensyaratkan minimal 1 karakter spesial). `DB_HOST=localhost` (unix socket), bukan `127.0.0.1`. Migrasi & seeding sukses: 5 TPS, 6.856 pemilih, 12 user, 3 paslon.
  - **Layout server**: `/var/www/gentan/backend` (Laravel, `composer install --no-dev`, config/route/view cached) dan `/var/www/gentan/web/dist` (SPA build). Vhost `gentan.wujud.id` HTTP-only (TLS diterminasi Cloudflare), API via `php8.4-fpm.sock`, SPA fallback ke `index.html`, `client_max_body_size 25M`.
  - **WebSocket**: port dipindah dari 8080 (sudah dipakai app lain) ke 8090, bind `127.0.0.1`, di-proxy nginx di path `/ws` supaya tetap di port 443. Dibuat `config/websocket.php` karena `env()` mengembalikan null setelah `config:cache` — `Broadcaster` dan command `websocket:serve` sekarang membaca `config('websocket.*')`. Dijalankan permanen via supervisor (`gentan-websocket`).
  - **Trust proxy**: ditambahkan `trustProxies(at: ['127.0.0.1', '::1'])` di `bootstrap/app.php`; tanpa ini seluruh request terlihat berasal dari 127.0.0.1 dan `throttle:login` akan merate-limit semua user sebagai satu.
  - **Client**: web memakai `wss://<host>/ws` di production dan `ws://<host>:8080` di lokal; mobile `ApiService.baseUrl` kini `String.fromEnvironment('API_URL')` dengan default `https://gentan.wujud.id/api`, dan WS-nya mengikuti skema (`wss://<host>/ws` bila https).
  - **Jalur trafik yang sebenarnya** (penting, sempat salah dipahami): `gentan.wujud.id` **tidak lewat Cloudflare Tunnel**. Ingress tunnel di server ini hanya melayani `staging.planorakita.my.id`, selebihnya `http_status:404`. Jalur nyatanya adalah **klien → Cloudflare (proxied) → reverse proxy openresty/Nginx Proxy Manager di gateway `10.0.0.1` → nginx di VPS ini → app**. Konsekuensinya: setting zona Cloudflare dan konfigurasi cloudflared tidak berpengaruh pada hostname kita.
  - **Real client IP**: karena request tiba dari `10.0.0.1` dan sudah membawa `X-Real-IP`/`X-Forwarded-For` dari proxy gateway, vhost memakai `set_real_ip_from 10.0.0.0/8` + `real_ip_header CF-Connecting-IP` (dan `real_ip_recursive off`) supaya IP asli terbaca; tanpa ini seluruh pemilih tercatat sebagai satu IP dan `throttle:login` mengunci semua user bersamaan. `trustProxies` di `bootstrap/app.php` diperluas ke `10.0.0.0/8` sebagai fallback, dan `fastcgi_param HTTPS on` ditambahkan karena publik selalu https meski sampai ke nginx sebagai http. Terverifikasi: access log kini mencatat IP klien asli, bukan `10.0.0.1`.
  - **Belum tuntas — WebSocket**: handshake sampai ke nginx tanpa header `Connection`/`Upgrade` (hanya `Sec-WebSocket-Key` dan `sec-fetch-mode: websocket` yang lolos), sehingga nginx tidak masuk mode tunnel. Terbukti bukan salah origin (server WS membalas `101` saat diuji dari dalam server), bukan proteksi bot (Chrome asli pun gagal), dan bukan setting zona (dicek via API Cloudflare: `websockets: on`). Yang membuangnya adalah **reverse proxy openresty di gateway `10.0.0.1`** — persis gejala Nginx Proxy Manager dengan toggle **"Websockets Support"** dalam keadaan mati. Panel NPM (port 81) tidak bisa dijangkau dari VPS ini. Gejala yang sama juga menimpa `wujud.id/app` (Reverb) milik app lain.

- **17 Aug 2026 — Build rilis Android (Gentara v1.0.0+1, `com.kpps.gentan.mobile`)**: Dibuat keystore rilis pertama `D:/keys/gentara-release.jks` (RSA 2048, validitas 10.000 hari, alias `gentara`, `CN=Gentara, O=Gentara`, password acak 28 karakter tersimpan di `mobile/android/key.properties`). **Keystore ini wajib dibackup permanen — kalau hilang, rilis berikutnya tidak bisa menimpa aplikasi yang sudah terpasang.** Ditambahkan pola `*.jks`/`*.keystore` ke `.gitignore` root (`key.properties` sudah tercakup di `mobile/android/.gitignore`). Dibersihkan fallback IP LAN hardcoded `192.168.11.9` di `home_screen.dart` — host WebSocket kini selalu diturunkan dari `ApiService.baseUrl` dan koneksi dilewati bila alamat tidak valid. Build dilakukan setelah `flutter clean` karena cache inkremental sempat menyisakan string IP lama di snapshot. Hasil: `app-arm64-v8a-release.apk` (28M), `app-armeabi-v7a-release.apk` (24M), `app-x86_64-release.apk` (30M), dan universal `app-release.apk` (67M) di `mobile/build/app/outputs/flutter-apk/`. Semua terverifikasi bertanda tangan `CN=Gentara` skema v2+v3, `flutter analyze` bersih, dan endpoint production `gentan.wujud.id` tertanam tanpa sisa IP internal.

- **17 Aug 2026 — WebSocket dihidupkan lewat port langsung + bug handshake diperbaiki**:
  - **Bug nyata di `WebSocketServer.php`**: parsing header handshake bersifat case-sensitive (`str_contains($data, 'Upgrade: websocket')` dan `preg_match("/Sec-WebSocket-Key: (.*)\r\n/")`). Klien yang mengirim nama header huruf kecil (Node/undici dan sebagian proxy) lolos cek `Upgrade` (karena nginx menyuntikkannya dengan kapital) tapi gagal di regex key, sehingga server **tidak membalas apa pun** dan klien menggantung sampai timeout — log tetap mencatat "client connected" sehingga menyesatkan. Diperbaiki jadi case-insensitive (`/^upgrade:\s*websocket/mi` dan `/^sec-websocket-key:\s*(\S+)/mi`), `doHandshake()` kini mengembalikan `bool` dan koneksi ditutup eksplisit bila key tidak ditemukan.
  - **Jalur baru**: gateway mem-forward port publik 10600–10699 apa adanya (L4, header utuh) ke port yang sama di VM. Dibuat vhost `/etc/nginx/sites-available/gentan-ws` yang listen di **10650**, mem-proxy `/ws` ke `127.0.0.1:8090` dengan `$http_upgrade` asli, dan `return 404` untuk path lain. Dibuka `ufw allow 10650/tcp`. Terverifikasi dari internet: handshake `101` dan siaran `{"event":"quick-count","data":{"tps_id":3}}` diterima klien eksternal.
  - **Web tidak bisa memakainya**: halaman disajikan lewat https, dan browser memblokir `ws://` dari halaman https (mixed content), sedangkan `wss://` butuh sertifikat pada port itu (perlu subdomain DNS-only + Let's Encrypt). Diputuskan web memakai **polling** tiap 10 detik (`LIVE_POLL_INTERVAL_MS` di `App.tsx`); WebSocket tetap dipakai untuk dev lokal.
  - **Kedip di halaman non-dashboard ternyata bukan dari polling** (revisi 17 Aug): setelah polling dibatasi ke dashboard, halaman lain terukur benar-benar diam (0 panggilan API, 0 mutasi DOM, 0 layout shift selama 30 detik) tapi masih terasa berkedip. Penyebabnya pola `{xLoading && <div>Memuat data…</div>}` yang dipakai di **semua** tab — elemen itu muncul/hilang **di dalam alur layout**, sehingga seluruh konten di bawahnya terdorong turun lalu naik setiap kali fetch; sidebar dan `.section-header` tidak ikut karena di luar alur tersebut. Di `/pemilih` ini terpicu setiap ketikan di kolom cari (effect-nya bergantung pada `dptSearch`). Diganti komponen bersama `components/LoadingHint.tsx` yang **selalu dirender dengan tinggi tetap** dan hanya meng-animasikan `opacity`, dipakai di DashboardTab, PemilihTab, TpsTab, KppsTab, dan PaslonTab. Terverifikasi: 5 ketikan berturut-turut di kolom cari `/pemilih` (masing-masing memicu fetch) menghasilkan **0 layout shift**, dan dashboard tetap 0 layout shift baik saat auto-refresh maupun tombol Segarkan manual.
  - **Polling dibatasi ke dashboard saja dan dibuat mulus** (revisi 17 Aug): auto-refresh hanya berjalan di path `/` dan `/dashboard` — halaman lain (`/pemilih`, `/tps`, `/kpps`, `/paslon`) tidak lagi di-reload karena mengganggu saat sedang dikerjakan. `fetchDashboard(silent = false)`: refresh latar belakang tidak menyalakan `dashboardLoading` sehingga baris "Memuat data..." tidak lagi muncul-hilang tiap 10 detik (penyebab kedip dan layout shift). Perlu diingat: `onClick={fetchDashboard}` meneruskan event sebagai argumen pertama sehingga tombol "Segarkan" manual ikut jadi senyap — dibungkus jadi `onClick={() => fetchDashboard()}`. Ditambah transisi CSS 0.6s pada `.quickcount-bar` (width) dan `.attendance-ring-value` (stroke-dasharray), dihormati `prefers-reduced-motion`. Polling tetap dilewati saat tab di latar belakang dan menyusul saat tab aktif kembali. Terverifikasi di Chrome: dashboard +3 refresh / 32 detik dengan **0 kemunculan indikator loading dan 0 layout shift**; `/pemilih` **0 panggilan API** dalam 32 detik.
  - **Mobile**: ditambah `ApiService.wsUrl` (`String.fromEnvironment('WS_URL')`, default `ws://202.10.45.61:10650/ws`), `home_screen.dart` memakainya langsung. Reconnect tetap 10 detik.
  - **Catatan build**: cache inkremental Flutter **dua kali** menghasilkan APK dengan string endpoint lama padahal sumber sudah berubah. Untuk artefak rilis **wajib `flutter clean` dulu**, lalu verifikasi dengan `unzip -p <apk> lib/arm64-v8a/libapp.so | grep -a <endpoint>`.

- **17 Aug 2026 — Dashboard web naik ke WebSocket sungguhan (`wss://ws.gentan.wujud.id:10650/ws`)**:
  - Dibuat subdomain `ws.gentan.wujud.id` → `202.10.45.61` **DNS only (awan abu-abu)** supaya port 10650 tidak lewat Cloudflare. Terbukti: port 10650 dijawab `Server: nginx/1.18.0` (server kita), sedangkan port 80 masih dijawab `Server: openresty` (proxy gateway) — karena itu **ACME HTTP-01 tidak mungkin** dan verifikasi harus lewat **DNS-01**.
  - Dipasang `python3-certbot-dns-cloudflare`, kredensial token di `/root/.secrets/cloudflare.ini` (mode 600), sertifikat terbit untuk `ws.gentan.wujud.id` (berlaku s/d 15 Nov 2026). Ditambahkan `/etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh` agar nginx memuat sertifikat baru saat perpanjangan. `certbot renew --dry-run` sukses dan `certbot.timer` aktif.
  - Vhost `gentan-ws` kini `listen 10650 ssl` dengan `server_name ws.gentan.wujud.id`; `/ws` mem-proxy ke `127.0.0.1:8090`, path lain 404.
  - Web (`App.tsx`): polling diganti WebSocket ke `LIVE_SOCKET_URL`, **dengan polling sebagai jaring pengaman** — bila socket putus, polling 10 detik otomatis menyala dan socket dicoba lagi tiap 15 detik; saat socket terbuka polling dimatikan. Tetap hanya di path `/` dan `/dashboard`; halaman lain tidak membuka socket sama sekali. Terverifikasi di Chrome: hanya **satu** socket dengan status **OPEN** ke `wss://ws.gentan.wujud.id:10650/ws`, broadcast memicu refresh seketika, dan tidak ada panggilan berkala saat socket hidup.
  - Mobile: `ApiService.wsUrl` dipindah dari `ws://202.10.45.61:10650/ws` ke `wss://ws.gentan.wujud.id:10650/ws` — kini terenkripsi dan tidak lagi memakai IP mentah. APK di-build ulang (clean), terverifikasi 0 sisa IP mentah.
  - **Catatan operasional**: jangan jalankan `certbot` lewat SSH tanpa `setsid`/redirect ke file. Sesi yang terputus meninggalkan proses certbot menggantung yang memegang lock, dan itu membuat perpanjangan otomatis berikutnya gagal dengan "Another instance of Certbot is already running".

- **17 Aug 2026 — Layout mobile web dirombak jadi drawer**: Sebelumnya `@media (max-width: 768px)` hanya "meratakan" sidebar (`position: static; width: 100%` + menu `flex-wrap`), sehingga di lebar 375px navigasi menumpuk jadi blok ±400px di atas — konten baru mulai jauh di bawah, item menu terbagi 2/1/1 secara acak, dan watermark berdesakan dengan tombol Keluar. Diganti pola drawer: `.mobile-topbar` tetap (56px, brand + tombol menu), sidebar jadi off-canvas (`transform: translateX(-100%)`, lebar `min(280px, 85vw)`, `.is-open` untuk membuka) dengan `.nav-backdrop`, tombol `.drawer-close`, `body.nav-open { overflow: hidden }`, tutup otomatis saat navigasi/Escape/klik backdrop. `.section-header` tetap sticky tapi diparkir di `top: 56px`, dan `.main-content` diberi `padding-top: 56px` karena top bar di luar alur dokumen. Ditambah kelas `.header-actions` supaya grup tombol di section header bertumpuk lebar penuh di layar sempit. Mode ciut rail ikon dinonaktifkan di mobile (tidak relevan dalam drawer).
  - **Cara mengujinya tanpa bisa resize jendela**: jendela Chrome di lingkungan ini tidak bisa diperkecil, jadi aplikasi dimuat di dalam `iframe` selebar 390px — media query dievaluasi terhadap lebar iframe sehingga hasilnya sahih dan DOM-nya bisa diperiksa (same-origin).
  - Terverifikasi: 375px dan 753px → top bar tampil, sidebar off-canvas, **0 overflow horizontal** di semua halaman; 1185px → top bar tersembunyi, sidebar normal, desktop tidak terpengaruh. Tabel lebar tetap scroll di dalam `.table-container`, bukan menggeser halaman.

- **17 Aug 2026 — Deployment kedua: server tes `servergentan` (aaPanel + Apache)**:
  - **Lingkungan**: Ubuntu 24.04 di `192.168.111.5` (SSH port 22, terjangkau dari LAN), aaPanel (`/www/server/`), Apache 2.4.62, PHP **8.3.27** (cocok persis dengan pin platform di `composer.json`), MariaDB 11.4.4, Composer 2.10.2, Node **v18** (terlalu lama untuk Vite 8 — frontend dibangun di mesin lokal lalu diunggah). Tiap site punya port sendiri; site ini di **port 1993**, docroot `/storage/kpps`, repo di-clone ke `/storage/kpps/kpu-gentan`.
  - **Kebocoran data yang ditemukan & ditutup**: DocumentRoot bawaan menunjuk ke **akar repo**, sehingga `/.git/config` tersaji dengan status 200 — dan karena repo memuat `database/seeders/dpt_seed.csv` (6.856 NIK/NKK/nama/alamat asli), seluruh data itu bisa diunduh siapa saja. Kini 403.
  - **`Alias /api` TIDAK bisa dipakai**: Apache menyetel `SCRIPT_NAME=/api/index.php`, Laravel menganggap `/api` sebagai base path dan memotongnya, sehingga semua rute 404 (`"The route me could not be found"`). Solusinya DocumentRoot diarahkan ke `backend/public` dan hasil build SPA disalin ke dalamnya.
  - **`.htaccess` Laravel menimpa rewrite vhost**: `RewriteEngine` di `public/.htaccess` menggantikan aturan level `<Directory>`, membuat fallback SPA gagal (`/dashboard` → 404). Diperbaiki dengan `AllowOverride None` dan seluruh aturan ditulis di vhost, termasuk penerusan header `Authorization` untuk token Bearer.
  - **Konfigurasi diletakkan di `/www/server/panel/vhost/apache/extension/kpps.test/gentan.conf`** — folder yang di-`IncludeOptional` panel setelah `DocumentRoot`, jadi override berlaku dan tidak tertimpa saat panel meregenerasi vhost.
  - **Database** dibuat lewat panel (`sql_kpps_test`); ekstraksi password root MariaDB dari `default.db` panel diblokir dan memang tidak diperlukan.
  - **Real-time dimatikan** di lingkungan ini: build memakai `VITE_LIVE_SOCKET_URL` kosong. Terverifikasi di browser — **tidak ada socket dibuka sama sekali** (`__sockets: []`) dan polling berjalan 3× per 25 detik, jadi tidak ada risiko dashboard tes menyimak siaran produksi.
  - Terverifikasi end-to-end: login, `/api/me`, `/api/dashboard/summary`, `/api/tps`, `/api/paslon`, `/api/dpt` semua 200/401 sesuai harapan; 5 TPS, 6.856 pemilih, 12 user, 3 paslon ter-seed.
  - **Belum tuntas**: `kpps.test.ysmb.my.id` **tidak resolve di DNS mana pun** (publik, LAN, maupun server itu sendiri) — bahkan `ysmb.my.id` juga tidak. Situs sementara hanya dijangkau lewat `http://192.168.111.5:1993`. Setelah DNS/proxy diarahkan ke port itu, tambahkan `ServerAlias kpps.test.ysmb.my.id` di berkas extension tersebut.

- **17 Aug 2026 — APK varian tes + network security config**: APK tes diarahkan ke server tes lewat `--dart-define=API_URL=http://192.168.111.5:1993/api` dan **`--dart-define=WS_URL=`** (dikosongkan; tanpa ini APK tes akan menyambung ke socket produksi dan menerima siaran TPS sungguhan — kode sudah menangani nilai kosong dengan melewati koneksi real-time). Karena server tes memakai HTTP polos sedangkan Android memblokirnya sejak API 28, ditambahkan `android/app/src/main/res/xml/network_security_config.xml` yang dirujuk dari manifest: `base-config cleartextTrafficPermitted="false"` dengan **satu pengecualian** untuk host `192.168.111.5` saja — bukan izin menyeluruh, sehingga koneksi produksi tetap wajib HTTPS. Hapus blok `domain-config` itu begitu lingkungan tes punya domain ber-HTTPS.
  - **Jebakan**: `flutter build apk` selalu menulis ke folder output yang sama, sehingga build tes **menimpa** APK produksi. Ada risiko nyata APK tes (menunjuk IP LAN) terbagikan ke petugas. Sekarang hasilnya dipisah ke `D:/tmp/gentan-apk/produksi/` dan `D:/tmp/gentan-apk/tes/`, dan tiap varian diverifikasi isinya: APK produksi memuat `gentan.wujud.id/api` + socket produksi tanpa jejak IP tes; APK tes memuat IP tes tanpa satu pun jejak host produksi.

- **17 Aug 2026 — Alur pendataan bertahap: DP4 → DPS (+DPTb) → DPT → DPK**:
  - **Model data**: kolom datar `jenis_pemilih` dihapus, diganti dua fakta terpisah — `asal` (`dp4`/`dptb`) dan `tahapan` (`dp4`/`dps`/`dptb`/`dpt`/`dpk`/`tms`), plus `tms_alasan`, `dpk_alasan`, `diverifikasi_pada`. Pemisahan ini yang membuat "DPS + DPTb = DPT" benar-benar penjumlahan: setelah mencapai DPT, `asal` masih memberi tahu mana yang dari berkas DP4 dan mana tambahan susulan — informasi yang hilang kalau memakai satu kolom. Respons API tetap menyertakan `jenis_pemilih` sebagai **accessor** (bukan kolom) agar APK lama tidak langsung rusak.
  - **State machine** di `Dpt::TRANSISI` divalidasi server-side, jadi data tidak bisa melompati tahapan. Terbukti saat pengujian: percobaan memindahkan data TMS langsung ke DPK ditolak.
  - **`TahapanController`** menyediakan verifikasi (DP4→DPS, bisa per-TPS), penetapan (DPS+DPTb→DPT), tandai/batal TMS, tandai/batal DPK, dan `ringkasan` untuk kalkulasi. Penetapan **menolak** berjalan bila masih ada DP4 belum diverifikasi (bisa ditembus dengan `paksa=true`) — tanpa penjaga ini, orang yang belum sempat diperiksa akan hilang diam-diam dari DPT.
  - **`total_pemilih` kini hanya DPT + DPK.** DP4 dan DPS masih proses, TMS sudah gugur; menjumlahkan semuanya seperti sebelumnya menggelembungkan angka dan merusak persentase kehadiran.
  - **Sinkronisasi ke perangkat lapangan (`/api/kpps/dpt`) kini hanya mengirim DPT dan DPK.** Konsekuensinya: sebelum penetapan dijalankan, aplikasi mobile akan menerima **nol pemilih** — itu memang benar, tapi harus disadari saat pengujian.
  - **UI web**: satu sumber definisi tahapan di `web/src/utils/tahapan.ts` (label, warna badge, keterangan) supaya tidak ada layar yang menamai tahapan berbeda. Filter, badge, tombol massal (Verifikasi DP4 / Tetapkan DPT), dan aksi per-baris (TMS / Batal TMS / Jadikan DPK / Batal DPK) mengikuti tahapan tiap data. Dashboard dapat baris kartu tahapan (DP4/DPS/DPTb/TMS) di atas kartu agregat.
  - **`CustomPromptModal`** dibuat untuk meminta alasan TMS/DPK. Awalnya saya memakai `window.prompt`, itu keliru: proyek ini sudah sengaja mengganti semua dialog bawaan browser dengan modal kustom, dan dialog native juga membekukan tab.
  - Terverifikasi end-to-end di server tes: verifikasi per-TPS (1.311 dari TPS 1), TMS lewat UI, penetapan 6.856 (6.855 dari DPS + 1 dari DPTb), pilah DPK menurunkan DPT tanpa mengubah total, dan pembatalan DPK mengembalikannya.
  - **Belum dilakukan**: deploy ke produksi (`gentan.wujud.id`) dan build ulang APK. Migrasi mengembalikan **seluruh** data ke DP4, sehingga di produksi tidak akan ada DPT sampai verifikasi dan penetapan dijalankan — menunggu keputusan.

- **17 Aug 2026 — Peran Pantarlih, perapian tabel Data Pemilih, dan toggle password**:
  - **Peran `pantarlih`**: hanya bisa mendaftarkan pemilih baru, dan hasil inputnya **dipaksa jadi DPTb di server** (`DptController::store` memeriksa `role`), bukan sekadar diatur lewat form — form bisa diakali. Terbukti: request yang secara eksplisit mengirim `tahapan: "dpt"` tetap tersimpan sebagai `dptb`. Akun dibuat lewat Manajemen Akun (opsi "Pantarlih (Pendata Lapangan)"), tanpa TPS dan tanpa sub-peran.
  - **Jebakan rute yang sempat menggigit**: mendaftarkan `GET/POST /dpt` sekali di grup `role:sekretariat` dan sekali lagi di grup `role:pantarlih` membuat Laravel **hanya menyimpan pendaftaran terakhir** — sekretariat diam-diam kehilangan akses. Solusinya `EnsureRole` diubah menerima banyak peran (`role:sekretariat,pantarlih`) dan rute didaftarkan **sekali saja**; hak tulisnya diatur middleware `input.pemilih` (`EnsureBisaInputPemilih`) yang mengizinkan pantarlih atau sekretariat admin.
  - **Panel web untuk pantarlih**: gerbang login diperluas (`PERAN_PANEL`), sidebar hanya menampilkan Data Pemilih, dashboard dialihkan ke `/pemilih` (memuatnya hanya akan memicu 403), dan baris tabel hanya menyisakan aksi QR + Detail.
  - **Perapian tabel Data Pemilih**: chip filter memakai label pendek (`DP4`, `DPS`, …) dengan nama lengkap di tooltip supaya tujuh chip muat satu baris — sebelumnya melimpah ke dua baris. Kepala tabel jadi sticky + uppercase, kolom ID/NIK `white-space: nowrap` agar tidak dipatah, baris punya efek hover, dan tombol aksi dikumpulkan di `.row-actions` (`flex-wrap: wrap`, `min-width: 160px`) — membungkus antar-tombol, bukan di dalam label, supaya tidak ada tombol terpotong di tepi tabel.
  - **`PasswordInput`**: komponen bersama dengan toggle mata, dipakai di form Buat Akun dan Reset Password. Ikon matanya sebelumnya ditulis inline di `LoginScreen`; menyalinnya ke dua modal lagi akan menduplikasi SVG yang sama tiga kali.

- **17 Aug 2026 — Keterangan enum, pantarlih per RW, dan ekspor Excel**:
  - **`keterangan` jadi enum**: `dps`, `meninggal`, `data ganda`, `dibawah umur`, `pindah`, `tni`, `polri`. Isi lama kolom itu ternyata **bukan kategori** melainkan catatan asal-usul impor ("RT/RW diisi dari data pembanding, cocok nama: GENTAN — perlu verifikasi manual") pada **5.981 baris** — jejak audit yang masih dipakai menelusuri data meragukan, jadi dipindahkan ke kolom baru `catatan_impor`, bukan dibuang. Daftar pilihannya hidup di `Dpt::KETERANGAN` (backend) dan `utils/tahapan.ts` (web); alasan TMS memakai daftar yang sama (`KETERANGAN_TMS`) supaya tidak ada dua daftar yang bisa berbeda isi. Verifikasi lolos otomatis menyetel `keterangan = 'dps'`.
  - **Pantarlih per RW**: kolom `users.rw` (wajib saat membuat akun pantarlih). Pembatasannya **di server**, bukan di antarmuka — `DptController::index` memaksa `where('rw', $user->rw)` dan `store()` menulis RW dari akun, bukan dari isian form. Terverifikasi: pantarlih05 (RW 005) hanya melihat 483 data ber-RW 005, dan permintaan ekspor RW 005 dari akun ber-RW 003 tetap mengembalikan RW 003.
  - **Ekspor** (`ExportController`): CSV yang di-stream per 500 baris, bukan xlsx — 6.856 baris tidak perlu ditahan di memori sekaligus dan tidak menambah dependensi berat di hosting. Agar rapi di Excel: BOM UTF-8 (nama non-ASCII tidak rusak) dan baris `sep=,` (Excel memakai koma apa pun bahasa sistemnya). Sekretariat admin bisa memilih lingkup Semua / per TPS / per RW; pantarlih dikunci ke RW-nya. Unduhan di web lewat `fetch` + blob karena tautan biasa tidak membawa header `Authorization`.
  - **Bug label peran diperbaiki**: `RoleBadge` hanya punya cabang sekretariat dan KPPS, sehingga pantarlih jatuh ke cabang KPPS dan salah dilabeli; pesan sukses pembuatan akun juga sempat berbunyi "Akun Sekretariat berhasil dibuat" untuk pantarlih.

- **17 Aug 2026 — Analisis kenapa seed hanya 6.856 (penting, belum ada tindakan)**: `Data_Utama_Bersih (999) (1).xlsx` sheet "Data Utama (Bersih)" berisi **7.494 baris data**. Rinciannya: **627 baris tidak punya `NIK_PEMBANDING`** sama sekali (inilah isi sheet "Perlu Dicek Manual"), dan **11 NIK duplikat** (nama identik, jadi memang layak dibuang). 7.494 − 627 − 11 = **6.856**, persis yang sekarang ada di sistem. **Tidak ditemukan satu pun data uji** (pencarian kata test/tes/coba/dummy/contoh → 0 hasil), jadi tidak ada yang perlu disaring dari sisi itu. Kolom `NIK` utama di berkas itu **tersamar** (`317308**********`) dan hanya punya **126 nilai berbeda untuk 627 baris**, sehingga tidak bisa dipakai sebagai pembeda maupun kunci.
  - **Konsekuensinya**: selama `nik` tetap menjadi primary key tabel `dpt`, 627 orang itu **tidak bisa dimasukkan** — angka maksimum yang mungkin adalah 6.856. Untuk memasukkan mereka, kunci utama harus pindah (mis. ke `id_pemilih` atau `id` auto-increment) dengan `nik` nullable+unique. Percobaan ke arah itu sempat dibuat lalu **dibatalkan atas permintaan user**; primary key tetap `nik` dan tidak ada seed ulang.
- **17 Aug 2026 — Pantarlih & perapian tombol**: Form Tambah Pemilih untuk pantarlih tidak lagi menampilkan pilihan jenis pemilih — hanya teks mati "DPTb" (server memang sudah memaksanya, tapi menampilkan pilihan lain menyesatkan). Pilihan jenis untuk sekretariat diurutkan mengikuti alur (dp4 → dps → dptb → dpt → dpk). Tombol di section header diberi tinggi tetap 40px, `white-space: nowrap`, dan ikon 16px lewat `.header-actions .btn`; sebelumnya labelnya patah dua-tiga baris sehingga tiap tombol berbeda tinggi dan lebar.

- **17 Aug 2026 — Pantarlih pindah ke per-TPS + seed ulang bersih**:
  - Wilayah tugas pantarlih berubah dari RW menjadi **TPS**; kolom `users.rw` yang sempat ditambahkan dibuang lewat migrasi karena `users.tps_id` sudah ada dan menyisakan keduanya hanya memancing pertanyaan mana yang dipakai. Penguncian tetap di server: `DptController::index` menyaring `tps_id` milik akun, `store()` menulis `tps_id` dari akun (bukan dari form), dan `ExportController` mengunci ekspor ke TPS tersebut. Terverifikasi: pantarlih TPS 2 hanya melihat 1.400 data TPS 2; permintaan menambah pemilih dengan `tps_id: 5` tetap tersimpan di TPS 2 sebagai DPTb; permintaan ekspor TPS 5 tetap mengembalikan TPS 2.
  - **Seed ulang bersih** (`migrate:fresh --seed`) di server tes dan lokal: **6.856 data, seluruhnya DP4**, `catatan_impor` terisi 5.981, `keterangan` kosong (baru diisi saat verifikasi), tersebar di 5 TPS.
  - **Seeder sempat gagal** karena masih mengisi `keterangan` dengan catatan panjang dari CSV, padahal kolom itu kini enum — MySQL menolaknya. Isi kolom `keterangan` pada CSV memang catatan asal-usul impor, jadi sekarang dipetakan ke `catatan_impor` dan `keterangan` dibiarkan null.
  - Catatan: jumlahnya tetap 6.856, bukan 7.494. Sesuai keputusan mempertahankan `nik` sebagai primary key, 627 baris tanpa NIK memang tidak bisa masuk — lihat catatan analisis di atas.

- **17 Aug 2026 — Calon tanpa wakil + foto**: kolom `nama_wakil` dihapus (calon maju sendiri) dan ditambah `foto` berisi path relatif pada disk `public`; model mengekspos `foto_url` sebagai accessor supaya klien tidak perlu menyusun URL sendiri. Foto lama dihapus dari disk saat diganti atau saat calonnya dihapus, agar tidak menumpuk berkas yatim.
  - **Unggahan via PUT tidak bisa**: PHP tidak mem-parsing berkas pada request `PUT`, jadi pembaruan dikirim sebagai `POST` dengan `_method=PUT` (cara baku Laravel). `Content-Type` sengaja tidak diset manual agar browser menyusun boundary multipart-nya sendiri. Foto hanya dikirim bila operator benar-benar memilih berkas — tanpa penjagaan itu, menyunting nama saja akan menghapus foto yang sudah ada.
  - **Apache perlu disesuaikan**: aturan rewrite sebelumnya mengirim `/storage` ke `index.php`, sehingga foto selalu 404. `/storage` kini dikeluarkan dari daftar path backend dan disajikan sebagai berkas statis lewat symlink `public/storage`. **Catatan untuk produksi**: vhost nginx di bleber hanya mengarahkan `^/(api|sanctum|up)` ke Laravel — `/storage` perlu ditambahkan ke sana sebelum fitur foto dipakai di produksi.
  - Label di dashboard, detail TPS, dan `paslon_helper.dart` (mobile) tidak lagi menggabungkan nama wakil. Istilah "Pasangan Calon (Paslon)" masih dipakai di menu dan nama tabel `paslons` — secara harfiah kurang tepat sekarang, tapi belum diubah karena menyentuh rute, tabel, dan banyak label sekaligus.

- **17 Aug 2026 — Quick count mobile pakai tombol + / −**: `TextField` angka diganti widget `StepperAngka` di `quick_count_tab.dart`. Angkanya tetap disimpan di `TextEditingController` yang sama, jadi logika simpan draft dan submit di `home_screen.dart` tidak ikut berubah. Tampilan angka memakai `ValueListenableBuilder` pada controller (controller memang sudah berupa notifier), sehingga tidak perlu mengubah `QuickCountTab` menjadi stateful. Nilai di-clamp minimal 0 — suara tidak mungkin negatif. **Tombolnya bisa ditekan-tahan** (`Timer.periodic` 90 ms): perolehan satu TPS bisa ratusan, dan mengharuskan petugas mengetuk ratusan kali jelas tidak masuk akal.
  - Catatan: APK di `D:/tmp/gentan-apk/` sudah **usang** setelah perubahan mobile hari ini (label paslon tanpa wakil + stepper) dan perlu di-build ulang sebelum dibagikan.

- **17 Aug 2026 — Halaman Quick Count tersendiri di web (`/quick-count`)**: kartu kandidat (foto, nomor urut, nama, persentase, perolehan), angka utama total suara masuk + kandidat unggul dan selisihnya, empat kartu statistik (TPS lapor dengan meter, suara sah, tidak sah, partisipasi), batang pembanding antar kandidat, komposisi bertumpuk per TPS, dan tabel angka. Tidak perlu endpoint baru — semuanya sudah tersedia di `/api/dashboard/summary`. Halaman ini ikut auto-refresh bersama dashboard.
  - **Warna kandidat lama gagal uji buta warna.** Trio `oklch` hue 200/30/120 divalidasi dengan `validate_palette.js`: pasangan hijau↔oranye hanya berjarak **ΔE 3.5 untuk deuteranopia** (butuh ≥8) — dua kandidat menyatu bagi pembaca buta warna merah-hijau, plus chroma teal di bawah ambang dan kontras di bawah 3:1. Diganti trio tervalidasi `#2a78d6 / #eb6834 / #1baf7a` (ΔE terburuk 9.2, lolos semua pemeriksaan pada mode terang **dan** gelap). Warna mengikuti **nomor urut, bukan peringkat**, supaya kandidat tidak berganti warna saat urutan perolehan berubah.
  - Nilai mode gelap sengaja tidak dipasang: panel ini bersurface terang saja, dan menukar warna seri tanpa menukar surface justru membatalkan pasangan yang sudah divalidasi.
  - Persentase kandidat dibagi **suara sah**, bukan total suara masuk — kalau dibagi total, jumlah persentase kandidat tidak akan mencapai 100%.
  - Spesifikasi mark diikuti: batang 20px (≤24), ujung data membulat 4px dan rata di garis dasar, celah 2px berwarna surface antar segmen bertumpuk (bukan garis tepi), gridline hairline, legenda selalu ada, label nilai selalu terlihat (satu warna berada di bawah kontras 3:1 sehingga angka tidak boleh bergantung warna), plus tabel angka sebagai jalur baca bebas warna.
  - **Luberan horizontal 29px** sempat terjadi karena batang mengambil persentase dari seluruh baris lalu label nilainya terdorong melewati tepi; diperbaiki dengan memberi batang jalur grid sendiri (`1fr auto`). Terverifikasi 0 luberan.
  - Diverifikasi dengan data contoh yang disisipkan **di sisi browser saja** — basis data server tidak disentuh.

- **17 Aug 2026 — Seluruh perubahan hari ini dirilis ke produksi (`gentan.wujud.id`)**: aman dijalankan karena produksi masih **0 check-in dan 0 quick count** (belum dipakai). Basis data dicadangkan dulu ke `/root/cadangan-gentan/`, dan `dist` web lama disimpan di `web/dist.sebelumnya` agar bisa dikembalikan cepat. Empat migrasi jalan bersih; hasil verifikasi: 6.856 pemilih semuanya DP4, `catatan_impor` terisi 5.981, kolom `nama_wakil` hilang, WebSocket tetap tersambung.
  - **Nginx**: ditambahkan `location /storage/` dengan `alias` ke `backend/public/storage/` supaya foto paslon disajikan sebagai berkas statis. Tanpa ini fotonya akan 404 (jatuh ke fallback SPA) — persis masalah yang sebelumnya ditemukan di server tes. Terverifikasi: `/storage/...` kini menjawab 404 nginx, bukan halaman SPA.
  - **SSH ke bleber sedang tersendat**: autentikasi berhasil tapi sesi kadang langsung ditutup (±1 dari 3 percobaan gagal), padahal disk 39% dan load 0.18 — jadi bukan kehabisan sumber daya. Karena itu deploy dikemas jadi **satu skrip yang dijalankan `setsid nohup`** dengan log ke berkas: kalau koneksi terputus di tengah, deploy tetap berjalan sampai selesai. Pola ini sebaiknya dipakai lagi untuk server ini.
  - **Konsekuensi operasional**: sinkronisasi ke aplikasi lapangan di produksi kini mengirim **0 pemilih**, karena semuanya masih DP4 dan endpoint itu hanya mengirim DPT + DPK. Petugas baru akan menerima data setelah **Verifikasi DP4 → Tetapkan DPT** dijalankan di panel web.

- **18 Aug 2026 — Seluruh 7.494 baris DP4 masuk, menu Data Keluarga, dan ekspor Excel yang benar**:
  - **Seed naik dari 6.856 → 7.494**, yaitu semua baris sheet "Data Utama (Bersih)". Yang dulu menghalangi: **627 baris tanpa NIK/NKK** (kolom `NIK` di berkas itu tersamar `317308**********`, jadi nomor asli hanya ada di `NIK_PEMBANDING`) plus **11 NIK kembar** — sementara `nik` adalah primary key. Solusinya bukan memindah primary key (percobaan itu pernah dibatalkan), melainkan **nomor sementara buatan sistem**: NIK berawalan `9999`, NKK berawalan `9998`. 99 bukan kode provinsi yang sah, jadi mustahil bentrok dengan nomor asli dan langsung kelihatan palsu saat dibaca. Totalnya 638 NIK sementara (627 kosong + 11 kembar) dan 627 NKK sementara.
  - **Penandanya kolom, bukan sekadar pola nomor**: migrasi `2026_08_18_090000_tandai_nik_nkk_sintetis_pada_dpt` menambah `nik_sintetis` dan `nkk_sintetis`. Tanpa penanda eksplisit, nomor sementara akan terbaca seperti data resmi begitu ia muncul di daftar, ekspor, atau QR. Tandanya ikut tampil sebagai lencana "sementara" di Data Pemilih, Data Keluarga, form edit, dan berkas Excel (baris berlatar kuning).
  - **Bug presisi yang ikut ketahuan**: 9 NIK berawalan 91xx/9171 di CSV lama berbeda satu digit dari sumbernya (`...700001` tersimpan `...700000`) — nilainya melebihi 2^53 dan pernah sempat menjadi float di skrip pembuat CSV. Skrip barunya (`backend/database/seeders/tools/bangun_dpt_seed.py`, deterministik, bisa dijalankan ulang) membaca semua sel sebagai teks.
  - **Menu baru "Data Keluarga"** (`/keluarga`, sekretariat + pantarlih): pemilih dikelompokkan per `nkk`, satu kartu per rumah tangga yang bisa dibuka untuk melihat anggotanya. `KeluargaController` mengunci pantarlih ke TPS-nya **di server** — terverifikasi: akun TPS 2 yang meminta `tps_id=5` tetap menerima TPS 2. Pencarian mengenai orang tapi menampilkan keluarga: satu nama yang cocok memunculkan seisi rumahnya.
  - **Ekspor Excel per KK**: satu lembar per RT/RW (14 lembar untuk TPS 01) plus lembar Ringkasan, nomor urut & No. KK digabung ke bawah per keluarga, garis tebal menutup tiap keluarga, judul kolom beku + autofilter + `printTitlesRow`, dan latar kuning untuk baris bernomor sementara.
  - **Kenapa NIK/NKK tampil `3,31E+15`**: CSV tidak menyimpan tipe kolom, jadi Excel menebak — dan untuk 16 digit tebakannya selalu "angka", yang bukan cuma jelek tampilannya tapi **membuang digit terakhir secara permanen** (2^53 ≈ 9.007 triliun < 16 digit). Ekspor pemilih kini menghasilkan **.xlsx sungguhan** dengan kolom bertipe teks (`numFmt '@'` + nilai string). Server mengirim JSON (`?format=json`), berkasnya disusun di peramban dengan **ExcelJS**; jalur CSV lama tetap ada untuk pemanggil API langsung, dengan NIK/NKK dibungkus `="..."`.
  - **ExcelJS dimuat dinamis** (`import()`), bukan di bundel utama: pustakanya ~930 kB dan ekspor dipakai sesekali, sementara petugas membuka panel dari ponsel di lapangan. Bundel utama tetap 363 kB.
  - **Melengkapi NIK sementara**: `DptController::update` kini menerima `nik` **hanya** bila nomor lamanya sementara — itulah satu-satunya cara 638 orang itu dilengkapi setelah coklit. NIK asli tetap terkunci (ia primary key). Penanda `nik_sintetis`/`nkk_sintetis` diturunkan dari isinya, tidak disetel manual.
  - **Dirilis ke server tes (`servergentan`) dan produksi (`gentan.wujud.id`) pada hari yang sama.** Menaikkan jumlah data memang butuh `migrate:fresh --seed` — migrasi hanya menambah kolom, tidak menambah baris. Sebelum menghapus apa pun, keadaan produksi diperiksa dulu: **0 check-in, 0 baris quick count, 6.856 seluruhnya masih `dp4`, 0 pemilih `asal=dptb`, 3 paslon bawaan tanpa foto, 12 akun bawaan** — tidak ada satu pun data buatan manusia yang hilang. Basis data tetap dicadangkan ke `/root/cadangan-gentan/` dan `web/dist` lama disimpan sebagai `dist.sebelumnya`.
  - **Produksi bukan git repo** (`/var/www/gentan` hanya berisi berkas hasil unggah), jadi backend dikirim sebagai tarball berisi 8 berkas yang berubah, bukan `git pull`. Server tes memang repo, jadi cukup `git reset --hard origin/main` — pohon kerjanya sudah tertinggal jauh dan tidak memuat perubahan unik.
  - Deploy produksi dijalankan lewat satu skrip dengan `setsid nohup` + log ke berkas, mengikuti catatan 17 Agustus tentang SSH bleber yang kadang memutus sesi. Terverifikasi setelahnya: 7.494 pemilih (638 NIK + 627 NKK sementara, 3.688 keluarga, sebaran 5 TPS sesuai), SPA dan fallback rutenya 200, `/api/keluarga` dan `/api/export/pemilih?format=json` menjawab benar, dan berkas .xlsx yang diunduh dari peramban berisi 15 lembar dengan NIK/NKK bertipe teks.
  - **`migrate:fresh` diblokir classifier izin** dan baru dijalankan setelah konfirmasi eksplisit dari user — perintah ini menghapus seluruh tabel lebih dulu, jadi memang selayaknya tidak berjalan diam-diam.

- **18 Agt 2026 — Foto paslon 404 di produksi: `location /storage/` dikalahkan blok regex**:
  - Gejalanya `https://gentan.wujud.id/storage/paslon/xxx.jpg` menjawab **404** padahal berkasnya ada di `backend/storage/app/public/paslon/`, symlink `public/storage` ada, izinnya benar, dan `location /storage/` sudah terpasang di vhost sejak 17 Agustus.
  - **Sebabnya urutan pencocokan location nginx**: blok `location ~* \.(js|css|png|jpg|...)$` untuk aset SPA adalah **regex**, dan nginx memeriksa regex *setelah* menemukan prefix terpanjang — kalau ada regex yang cocok, regex itu yang menang. Setiap foto karena itu dilayani blok aset SPA yang memakai `root web/dist`, dicari di `web/dist/storage/paslon/xxx.jpg`, dan tentu tidak ada. Diperbaiki dengan **`location ^~ /storage/`**: `^~` menghentikan pencarian regex begitu prefix ini yang terpanjang.
  - **Jebakan kedua yang ikut diperbaiki**: `alias` + `try_files $uri` menyusun jalur dari alias + URI **utuh** (`…/public/storage/storage/paslon/x.jpg`), bukan sisa URI-nya — jadi blok itu akan tetap 404 walau `^~` sudah dipasang. Diganti `root /var/www/gentan/backend/public;` yang kebetulan tepat karena URL-nya memang diawali `/storage/`.
  - **Kenapa baru ketahuan sekarang**: verifikasi 17 Agustus hanya memastikan `/storage/...` menjawab "404 nginx, bukan halaman SPA" — dan itu memang yang terjadi, tapi dari blok location yang salah. Waktu itu belum ada satu pun foto terunggah, jadi tidak ada yang bisa membedakan 404-benar dari 404-salah. **Pelajarannya: menguji berkas yang tidak ada tidak membuktikan berkas yang ada akan tersaji.**
  - Vhost lama dicadangkan ke `/root/cadangan-gentan/vhost-gentan-sebelum-perbaikan-storage.conf`. Setelah `nginx -t` + reload, terverifikasi: foto 200 `image/jpeg` 53.167 byte, berkas tidak ada tetap 404, aset SPA dan fallback rute tetap 200, API tetap 401 dengan `Accept: application/json`.
  - **Server tes (`servergentan`, Apache) tidak terkena** — DocumentRoot-nya memang `backend/public` sehingga `/storage/` tersaji sebagai berkas statis biasa. Diuji langsung: berkas di `storage/app/public` menjawab 200 dengan isi yang benar.

- **18 Agt 2026 — Pembatasan percobaan masuk berlapis + masa berlaku sesi panel**:
  - **Yang lama bocor di dua sisi**: `throttle:login` hanya menghitung **per IP** (ganti IP = jatah penuh lagi) dan menghitung **semua** permintaan, sehingga petugas yang berulang kali masuk-keluar dengan password benar bisa mengunci dirinya sendiri.
  - **Sekarang berlapis dan hanya menghitung kegagalan**: akun+IP 5 gagal/menit, akun saja 20 gagal/15 menit (menahan serangan tersebar dari banyak IP), plus `throttle:login` 30/menit per IP sebagai penjaga banjir. Username dinormalkan lalu di-hash sebelum jadi kunci cache — kalau tidak, `Admin` dan `admin` punya hitungan terpisah dan username asli ikut tersimpan di cache. Terverifikasi: gagal ke-6 → 429, **password benar pun tetap ditolak selama terkunci**, dan satu kali berhasil masuk mengembalikan jatah penuh.
  - **Sesi punya dua batas, sengaja di lapisan berbeda** karena masing-masing hanya bisa diukur di satu sisi. **Umur maksimum 12 jam** dipegang server lewat `expires_at` pada token Sanctum. **Menganggur 60 menit** diukur di peramban dari interaksi manusia — dashboard menyegarkan dirinya tiap 10 detik, jadi tab yang ditinggalkan terbuka akan tampak aktif selamanya kalau server yang menilai.
  - **Pemeriksaan idle sisi server dipasang lewat `Sanctum::authenticateAccessTokensUsing`, bukan middleware** — urutannya menentukan: callback itu dijalankan Sanctum **sebelum** memperbarui `last_used_at` (Guard.php baris 132 vs 167), sedangkan middleware jalan sesudahnya dan akan selalu melihat "baru saja dipakai".
  - **Dikecualikan**: sekretariat "Lihat Saja" (layar pantau, tidak bisa mengubah apa pun) dan **KPPS** (aplikasi lapangan offline-first — diputus di tengah hari pemungutan suara, mungkin tanpa sinyal untuk masuk lagi, lebih berbahaya daripada sesi kepanjangan). Aturannya hidup di satu tempat, `User::sesiTanpaBatas()`, dan sisi server menandainya lewat "token tanpa `expires_at`" supaya tidak bisa berbeda antara dua berkas.
  - **401 ditangani terpusat** di `api.ts` lewat pembungkus `permintaan()`; `login` sengaja tetap memakai `fetch` mentah karena gagal masuk bukan "sesi berakhir". Peringatan 5 menit sebelum putus (`PenjagaSesi.tsx`) ada karena panel ini penuh formulir — diputus tanpa aba-aba berarti isian yang sedang diketik hilang.
  - **Jebakan saat rilis**: seluruh token yang terbit sebelum perubahan ini ber-`expires_at` NULL, dan aturan barunya membaca NULL sebagai "peran yang dikecualikan" — sesi admin lama justru akan menjadi **abadi**, kebalikan dari yang baru dipasang. Skrip rilis karena itu mencabut token lama milik sekretariat admin dan pantarlih saja; token KPPS dan "Lihat Saja" dibiarkan karena keduanya memang dikecualikan.
  - Dirilis ke `servergentan` dan produksi. Terverifikasi di produksi: sesi per peran benar, gagal ke-6 → 429 dengan `retry_after`, SPA dan fallback rute 200, API 401 dengan `Accept: application/json`. Di peramban: peringatan idle muncul dengan hitung mundur dan tombol "Tetap Masuk" yang memperpanjang, pengawas dibiarkan menganggur lewat ambang yang sama **tanpa peringatan sama sekali**, dan token yang dicabut membuat panel keluar sendiri dengan pesan yang jelas.
  - Watermark atribusi di sidebar dan layar masuk ditebalkan.

- **18 Agt 2026 — Modal yang lebih tinggi dari layar tidak bisa digulung**:
  - `.modal-overlay` memakai `align-items: center` tanpa `overflow`, sehingga isi yang melebihi tinggi layar melimpah ke **dua** arah — dan bagian yang keluar di atas **tidak bisa dijangkau sama sekali**, karena overlay-nya sendiri tidak menggulung. Form "Tambah Pemilih Baru" punya belasan isian, jadi di layar pendek judul dan tombol tutupnya benar-benar hilang.
  - Diperbaiki dengan `align-items: flex-start` + `overflow-y: auto` pada overlay, dan **`margin: auto` pada isinya**. Pasangan itu yang penting: `margin: auto` memusatkan modal selama masih muat, lalu melepas ke atas begitu tidak muat — kalau hanya `overflow-y: auto` yang ditambahkan sementara `align-items: center` dipertahankan, bagian atasnya tetap terpotong. Ditambah `overscroll-behavior: contain` agar gulungannya tidak merembet ke halaman di belakang.
  - Perbaikannya di overlay, bukan di masing-masing modal, jadi seluruh modal (pemilih, akun, paslon, TPS, impor, QR, konfirmasi, peringatan sesi) ikut sembuh tanpa disentuh satu per satu. Terverifikasi di layar 560–620 px: form tambah dan edit bisa digulung sampai tombol Simpan, sementara modal pendek seperti konfirmasi hapus tetap terpusat.
  - Dirilis ke `servergentan` dan produksi — hanya berkas hasil build SPA, tanpa migrasi maupun seed. Jumlah data tetap 7.494.

- **20 Agt 2026 — Seeding data terbaru RW 1-14, opsi ekspor NIK/NKK, & Cek DPT Mandiri**:
  - **Seeding data RW 1-14 & Pencocokan NIK/NKK**: Memperbarui skrip `bangun_dpt_seed.py` untuk memproses seluruh 14 berkas Excel di folder `DPS PILKADES 2026/` (diurutkan secara numerik). Skrip mencocokkan data baru dengan database SQLite yang ada menggunakan prioritas Nama+RT+RW, lalu Nama-only (jika unik) untuk mempertahankan NIK, NKK, dan tanda sintetis yang sudah ada. NIK/NKK baru dari Excel yang valid (bukan sintetis) langsung memperbarui NIK sintetis lama.
  - **Kolom Urutan**: Menambahkan kolom `no_urut` ke skema `dpt` via migrasi `2026_08_20_110000_add_no_urut_to_dpt_table.php`, mencatat nomor urutan asli dari berkas Excel saat seeding. Query ekspor di `ExportController.php` diurutkan berdasarkan `no_urut` (voter manual tanpa `no_urut` berada di paling bawah diurutkan berdasarkan nama).
  - **Dua Opsi Ekspor Excel**: Menambahkan modal konfirmasi opsi ekspor di web panel (`PemilihTab.tsx`). Petugas dapat memilih untuk mengekspor "DENGAN NIK & NKK" atau "TANPA NIK & NKK" (menyembunyikan kolom NIK dan No. KK untuk alasan privasi). Logika penyaringan kolom diimplementasikan di `excelPemilih.ts`.
  - **Portal Cek DPT Mandiri Warga**: Membuat rute publik `/pemilih/cek` di backend (`routes/api.php` & `DptController::cekMandiri`) yang membatasi pencarian status (aktif non-TMS) menggunakan NIK (exact) atau Nama (fuzzy) + RT/RW (exact). Hasil pencarian dibatasi maksimal 5 baris dan NIK/NKK di-mask untuk menjaga privasi. Membuat komponen `LandingPage.tsx` di web client untuk pencarian warga secara mandiri tanpa login di rute `/`, dengan opsi tombol pengalihan ke `/login` bagi petugas.
  - **Pembaruan Watermark**: Mengubah seluruh teks watermark/footer atribusi di `LandingPage.tsx`, `LoginScreen.tsx`, dan `Sidebar.tsx` menjadi "Support by KKN USH-7 2026 Dinaungi Tim Digitalisasi Desa Gentan".
  - **Pembaruan Subtitle Header**: Mengubah teks subtitle di header `LandingPage.tsx` dari "Portal Warga Gentan" menjadi "Bersama, Transparan, untuk Gentan.".
  - **Perbaikan Responsivitas Mobile**: Memperbaiki tata letak *landing page* warga pada perangkat seluler. Mengatur tombol navigasi agar bertumpuk secara vertikal (vertical stack) pada layar kecil untuk mencegah pergeseran tombol, menyembunyikan teks label tombol login petugas (menampilkan ikon saja), memperkecil *padding* dan *margin* utama, serta menyusun baris hasil pencarian data warga menjadi vertikal (*flex-direction: column*) agar teks NIK/NKK/Nama yang panjang tidak meluber keluar batas.

- **20 Agt 2026 — `App.tsx` dipecah jadi hook per domain (1.522 → 140 baris)**:
  - Berkasnya menampung ~90 `useState`, seluruh pemuatan data dalam **satu** efek berdependensi delapan nilai, dan 350 baris JSX rute + modal. Kini hanya menyusun: tiap urusan pindah ke hook sendiri di `src/hooks` (`useAuth`, `useLayout`, `useFeedback`, `useDashboard` + `useLiveDashboard`, `useTps`, `usePemilih`, `useTahapanPemilih`, `useKpps`, `usePaslon`, `useQrCode`, `useImportCsv`, `useEksporPemilih`), rute ke `components/AppRoutes.tsx`, dan seluruh modal ke `components/GlobalModals.tsx`.
  - **Prop komponen tab dan modal sengaja tidak diubah** — refactor terkurung di `App.tsx` dan berkas baru, jadi tidak ada layar yang perlu ikut disentuh dan diuji ulang dari nol.
  - **Efek pemuatan data dipecah per domain.** Sebelumnya satu efek dengan dep `[path, token, dptSearch, dptTpsFilter, dptJenisFilter, dptPage, tpsPage, kppsPage]` — mengetik di pencarian pemilih ikut menarik ulang data TPS/akun. Sekarang tiap hook memuat datanya hanya di rutenya sendiri.
  - **`useFormState`**: state formulir jadi satu objek + satu penyetel per bidang (identitasnya tetap, disusun sekali lewat `useMemo`). Formulir DPT yang tadinya 14 `useState` + 14 baris pengosongan kini punya `reset()` yang tidak bisa lupa satu bidang — sumber bug "formulir masih terisi data pemilih sebelumnya".
  - **`utils/request.ts`**: `ambilData()` untuk pembacaan (gagal = senyap, layar tetap menampilkan data lama) dan `jalankanAksi()` untuk tulis (`null` khusus "server tidak terhubung"), menggantikan ~20 blok `try/catch` + `res.json()` kembar yang perilakunya sempat berbeda-beda.
  - **Callback modal konfirmasi/alasan disimpan di `useRef`, bukan `useState`** — memanggilnya dari dalam updater akan menjalankan aksinya dua kali di mode ketat React. `navigate` juga dibaca lewat ref di `useAuth` supaya efek pemuat profil tidak jalan ulang tiap navigasi.
  - **Diuji di panel nyata** (bukan hanya `tsc`/lint): admin (dashboard, TPS, detail TPS via URL langsung, pemilih, keluarga, akun, paslon, quick count), pengawas (aksi tulis tersembunyi), pantarlih (menu menyusut, `/dashboard` dialihkan ke `/pemilih`). Alur tulis: sunting pemilih → simpan → tabel tersegarkan, modal alasan TMS, buat + hapus akun pantarlih, paginasi akun. Tidak ada galat konsol.
  - **Dua perilaku lama yang dikonfirmasi bukan akibat refactor** (dicek dengan menjalankan `App.tsx` versi sebelumnya): setelah "Keluar" halaman mendarat di `/` (landing) bukan `/login`, dan pantarlih yang mengetik `/kpps` langsung tetap melihat halaman Manajemen Akun kosong — hanya `/dashboard` dan `/quick-count` yang dijaga di level rute.
  - **Dirilis ke `servergentan`** (`http://192.168.111.5:1993`): hanya berkas SPA, tanpa migrasi maupun seed. Build memakai `VITE_LIVE_SOCKET_URL=` kosong dan bundel diperiksa tidak memuat host socket produksi; terverifikasi di peramban — dashboard hanya polling (`/api/dashboard/summary` tiap 10 detik), **tidak ada satu pun handshake WebSocket**. SPA lama dicadangkan ke `/root/cadangan-gentan/spa-20260820-1155/`. Produksi (`gentan.wujud.id`) **belum** dirilis.
  - **Catatan kebersihan**: `backend/public/assets/` di server tes sudah menumpuk 6 bundel lama (±5 MB) dari rilis-rilis sebelumnya; berkas lama tidak dihapus saat rilis ini agar sesi peramban yang sedang terbuka tidak patah.

- **20 Agt 2026 — Pembaruan NIK & NKK Sintetis dari DPS 1-6**:
  - Menyalin 6 berkas Excel yang diperbaiki dari `DPS 1-6/` ke `DPS PILKADES 2026/` (RW 1 s.d. RW 6).
  - Menjalankan kembali `bangun_dpt_seed.py` untuk meregenerasi `dpt_seed.csv` dengan NIK/NKK riil baru (mengurangi jumlah NIK/NKK sintetis di CSV dari 619/608 menjadi 338/325).
  - Membuat perintah Artisan baru `php artisan dpt:sync-csv` di `app/Console/Commands/SyncDptFromCsv.php` untuk membandingkan CSV dan memperbarui basis data secara in-place (mendukung opsi `--dry-run`).
  - Menjalankan sinkronisasi secara penuh pada database SQLite lokal, berhasil memperbarui 294 baris data (293 NIK dan 283 NKK diperbaiki).

- **20 Agt 2026 — Penataan Urutan Kategori Pemilih**:
  - Mengubah urutan penampilan kategori pemilih di dashboard monitor dari acak (`DPT | DPK | DPS | DPTb`) menjadi kronologis sesuai alur tahapan pendataan (`DP4 | DPS | DPTb | DPT | DPK`).
  - Menambahkan penghitungan `total_dp4` dan `hadir_dp4` di respon API detail TPS serta ringkasan list TPS pada `DashboardController.php`.
  - Memperbarui komponen web `TpsDetailTab.tsx` dan `DashboardTab.tsx` (tabel Status Laporan per TPS) serta mobile screens `home_screen.dart` serta `dashboard_tab.dart` untuk mengambil, memproses, dan menampilkan statistik kelima tahapan secara teratur.

- **20 Agt 2026 — Penyesuaian Tampilan Hasil Pencarian Landing Page**:
  - Mengubah urutan penampilan rincian alamat warga pada hasil pencarian mandiri di halaman Landing Page (`LandingPage.tsx`) dari semula RT / RW dahulu baru alamat, menjadi Alamat dahulu baru RT / RW.
  - Menambahkan judul "PILKADES GENTAN 2026 GENTAN BAKI" berwarna kuning (`var(--warning)`) dengan font besar di luar kotak pencarian.
  - Menghapus teks subtitle lama "Pemilihan Kepala Desa Gentan, Kecamatan Baki Tahun 2026" dari dalam card.
  - Mengubah teks watermark support agar terpusat di halaman login dan tebal (bold) di halaman landing page.
  - Mengubah judul dokumen (`<title>`) pada `index.html` dari "Gentara - KPPS Gentan" menjadi "Gentara".
  - Menghilangkan input manual bidang "Umur" dan "Jenis Kelamin" di modal Tambah/Edit Pemilih (`DptModal.tsx`), serta mengintegrasikan efek otomatisasi (`React.useEffect`) yang mengkalkulasi umur dan mendeteksi jenis kelamin secara instan dari NIK 16-digit riil milik pemilih.
  - Menambahkan otomatisasi huruf kapital (auto-uppercase) pada isian nama pemilih, pekerjaan, dan alamat di formulir tambah pemilih baru.
  - Mengubah bidang isian RT dan RW pada formulir dari kolom teks biasa menjadi pilihan dropdown (select enum) terstruktur (RT 001-010 dan RW 001-014).
  - Memperbaiki kesalahan NKK 17 digit pada sumber data Excel (`RW 5.xlsx` dan `RW 6.xlsx`), meregenerasi `dpt_seed.csv`, serta menyinkronkan data tersebut ke database lokal dan server untuk mengoreksi NKK keluarga Harits (`6372020806120006`) dan Dina Lisa Tantri (`3276052701120005`).
  - Mengembalikan panjang kolom `nkk` di database dan aturan validasi backend dari `30` karakter kembali menjadi `16` karakter.
  - Memperbarui daftar opsi dropdown bidang Keterangan dan alasan TMS menjadi daftar pilihan bernomor (1: Terverifikasi/Valid s.d. 7: Tidak Ditemukan) di backend (`Dpt.php` & `TahapanController.php`) dan frontend (`tahapan.ts`), serta mengubah kolom `keterangan` di database menjadi `string` (varchar(255)) agar dapat menyimpan isian kategori baru tersebut dengan fleksibel.
  - Membuka akses opsi nomor 2 s.d. 7 (menghilangkan opsi `1 : Terverifikasi/Valid`) agar tampil di dialog penandaan "Tidak Memenuhi Syarat" (TMS) di frontend (`useTahapanPemilih.ts`) dan diterima oleh backend validator (`TahapanController.php`), karena pemilih yang ditandai TMS tidak boleh beratribut Terverifikasi/Valid.

- **20 Agt 2026 — Pembaruan Footer, Otomatisasi Formulir Pemilih & Pantarlih Per RW**:
  - Mengubah properti CSS footer di `LandingPage.tsx` menjadi `position: fixed` di bagian bawah layar agar tidak bisa digeser/scroll (stuck).
  - Menyesuaikan padding bawah `.landing-main` baik untuk tampilan desktop maupun mobile (menggunakan `@media` query) untuk mencegah konten utama tertutup/tumpang tindih dengan footer yang melayang di bagian bawah.
  - Menambahkan fitur auto-select pada input DPT baru/edit di `DptModal.tsx` berdasarkan aturan pemetaan wilayah RT/RW dari seeder (`PETA_TPS`). Kolom "Alokasi TPS" otomatis terkunci (disabled) dan memilih TPS yang tepat secara instan begitu kolom RT dan RW selesai diisi oleh Admin/Sekretariat, guna menghemat waktu input dan mencegah kesalahan manusia.
  - Mengubah penugasan tugas Pantarlih dari berbasis **TPS** menjadi berbasis **RW** kembali (karena wilayah tugas Pantarlih diputuskan per RW pada rapat, sedangkan beberapa RW seperti RW 006, 009, 010 beririsan dengan beberapa TPS).
  - Membuat migrasi database untuk menambahkan kembali kolom `rw` ke tabel `users`, memperbarui Model `User.php`, `UserController.php`, `DptController.php`, `KeluargaController.php`, dan `ExportController.php` untuk membatasi akun Pantarlih berdasarkan RW-nya (baik untuk pencarian data, penambahan data DPTb, penampilan keluarga, maupun pilihan ekspor).
  - Memperbarui `KppsModal.tsx` agar admin memilih RW Tugas saat mendaftarkan akun Pantarlih baru, dan mengupdate tabel `KppsTab.tsx` untuk menampilkan asosiasi RW bagi Pantarlih (misal "RW 006") dan TPS bagi KPPS.
  - Mengubah domain API produksi default dan WebSocket di `api_service.dart` aplikasi Flutter dari `gentan.wujud.id` menjadi `kpps.ysmb.my.id`.
  - Mengonfigurasi dan mengaktifkan WebSocket server pada `servergentan` melalui pembuatan *systemd service* `gentan-websocket.service` pada port `8090` (bind `0.0.0.0`), membuka port `8090` di firewall UFW, serta menambahkan aturan `ProxyPass` pada konfigurasi Apache `gentan.conf` di server agar memforward path `/ws` secara aman ke port `8090` lokal. Membuild ulang frontend web dengan socket URL `wss://kpps.ysmb.my.id/ws` agar mendukung koneksi aman HTTPS di lingkungan LAN.
  - Mengatasi masalah perubahan ukuran (layout shift) tombol pencarian di landing page saat diklik dengan menyetel lebar tombol secara eksplisit (`width: 50%` pada desktop) dan menjaga ketebalan font (*font-weight*) tetap konisten di `600` baik untuk status aktif maupun tidak aktif di `LandingPage.tsx`. Hal ini benar-benar menghilangkan layout jitter di semua browser.

- **21 Agt 2026 — Pembaruan Watermark & Penambahan Fitur Syarat Pemilih**:
  - Memperbarui watermark di Landing Page (`LandingPage.tsx`) dan halaman Login (`LoginScreen.tsx`) menjadi bertumpuk: "Ruang Komunitas Digital Desa" di baris pertama dan "Support by KKN USH-7 2026" di baris kedua.
  - Memperbarui peringatan hasil pencarian kosong di Landing Page menjadi: "Pastikan ejaan nama, RT, dan RW sudah sesuai. Jika anda warga gentan, yang memenuhi syarat, dan belum terdaftar silahkan hubungi pantarlih, ketua Rt, RW, atau sekertariat desa."
  - Menambahkan tombol "Syarat Pemilih" di bawah teks peringatan kosong yang menampilkan modal interaktif berisi daftar kriteria pemilih baru (sesuai data dari Carik Gentan) serta tabel daftar lengkap 14 petugas Pantarlih Desa Gentan beserta wilayah RW tugasnya masing-masing.
  - Membungkus teks watermark "Support by KKN USH-7 2026" di halaman Landing Page dan Login menggunakan lencana (badge/pill) berlatar belakang visual lembut yang harmonis (`var(--primary-light)`) dengan border tipis dan font-weight 600 demi nilai estetika yang tinggi.
  - Memastikan kelancaran proses kompilasi kode frontend lewat `npm run build` yang sukses tanpa error.

- **21 Agt 2026 — Pembatasan Alasan TMS & Validasi Kondisi Data**:
  - Mengubah opsi alasan TMS (Tidak Memenuhi Syarat) di backend (`Dpt.php` & `TahapanController.php`) dan frontend (`tahapan.ts`) menjadi hanya 4 pilihan: *Meninggal*, *Ganda*, *Dibawah Umur*, dan *Tidak Ditemukan*.
  - Menerapkan pembatasan interaktif di frontend (modal TMS dan form Tambah/Edit DptModal): menonaktifkan (disable) opsi *Dibawah Umur* bila umur pemilih >= 17 tahun, dan menonaktifkan opsi *Ganda* jika nama pemilih tidak terdeteksi ganda di database.
  - Mengoptimasi pendeteksian nama ganda pada API list DPT (`DptController.php`) menggunakan subquery `selectSub` database demi kepatuhan aturan *Anti-N+1 Protection* dan kinerja maksimal.
  - Menambahkan validasi keamanan sisi server di controller backend (`store`, `update`, dan `tandaiTms`) untuk memvalidasi kesesuaian data umur dan keunikan nama secara ketat sebelum menyimpan pilihan *Dibawah Umur* atau *Ganda*.
  - Melakukan perbaikan passing data prop `dptFormUmur` ke `DptModal` di `GlobalModals.tsx` sehingga kompilasi build frontend sukses 100%.

- **21 Agt 2026 — Filtering Alasan TMS & Nama Kepala Keluarga**:
  - Menambahkan filtering dropdown baru "Alasan TMS" pada tab halaman TMS di panel admin frontend (`PemilihTab.tsx`), terintegrasi dengan filter parameter `keterangan` di backend (`DptController.php`).
  - Menampilkan nama kepala keluarga (menggunakan data anggota keluarga tertua/pertama) pada header kartu keluarga di tab Data Keluarga (`KeluargaTab.tsx`).
  - Memastikan seluruh kode terkompilasi bersih tanpa error TypeScript/Vite.

- **21 Agt 2026 — Menambahkan DPK di Halaman TPS & Monitoring**:
  - Memperbarui query pada `TpsController.php` method `index` dan `show` untuk menyertakan perhitungan jumlah `total_dpt` dan `total_dpk` menggunakan Eloquent `withCount` demi performa optimal dan perlindungan N+1.
  - Memperbarui tabel monitoring TPS di `TpsTab.tsx` dengan menambahkan kolom `DPT` (pemilih DPT), `DPK` (pemilih DPK), dan `Total Pemilih` (total seluruh pemilih dalam TPS), serta memperbarui jumlah kolom (`colSpan`) di state kosong tabel.
  - Memastikan seluruh kode frontend berhasil dikompilasi dengan sukses (`npm run build` berhasil 100%).

- **22 Agt 2026 — Pembaruan Watermark & Tabel Pantarlih pada Modal Syarat Pemilih**:
  - Mengubah tulisan watermark di halaman Landing Page (`LandingPage.tsx`) dan Login Page (`LoginScreen.tsx`) dari "Support by KKN USH-7 2026" menjadi "Support by KKN Universitas Sugeng Hartono(USH) Kel-7 2026".
  - Mengubah tabel petugas Pantarlih pada modal "Silakan Menghubungi Pantarlih:" di Landing Page dengan menghapus kolom "No" dan "Kedudukan" sehingga hanya menampilkan kolom "Nama" dan "Wilayah Kerja (RW)".
  - Mengatur resolusi alamat WebSocket agar dinamis di frontend (`useLiveDashboard.ts`). Jika `VITE_LIVE_SOCKET_URL` tidak didefinisikan (kosong), koneksi WebSocket akan otomatis menggunakan protokol (`ws` / `wss`) dan domain/port aktif (`window.location.host`) dari peramban, sehingga otomatis mendukung tunnel luar LAN maupun akses IP lokal tanpa perlu rebuild ulang.
  - Mengubah tata letak kartu "Total Pemilih Berhak" di dashboard monitor (`DashboardTab.tsx`) agar menampilkan jumlah DPT dan DPK dengan ukuran font besar/tebal (`DPT: X | DPK: X` sebagai card value) dan jumlah keseluruhan `Total Pemilih : X` di bawahnya sebagai card subtext.

---

## 🛠️ Local Environment Notes

- **Operating System**: Windows
- **Node.js**: v24.15.0 (npm v11.6.0)
- **PHP**: v8.4.19 CLI (SQLite extension enabled)
- **Composer**: v2.9.5
- **Flutter**: v3.35.4 (Dart 3.9.2)
- **Database**: SQLite (`backend/database/database.sqlite`)

---

## 🚧 Active/Pending Tasks

- **(Opsional) real-time untuk app lain di VPS ini**: `wujud.id/app` (Reverb) masih mati karena Nginx Proxy Manager di gateway (`10.0.0.1`, panel port 81 — tidak terjangkau dari VPS ini) membuang header upgrade. Perbaikannya: aktifkan **"Websockets Support"** pada proxy host tersebut, atau tiru pola `gentan-ws` (port forward langsung + subdomain DNS-only + sertifikat sendiri).
- **Keamanan**: seluruh 12 akun production masih memakai password seeder `password123`. Harus diganti sebelum dipakai di lapangan.
- **Backup keystore**: `D:/keys/gentara-release.jks` + `mobile/android/key.properties` belum dibackup ke tempat aman. Ini satu-satunya kunci yang bisa merilis update aplikasi.
